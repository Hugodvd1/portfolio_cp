{% extends 'base.html.twig' %}

{% block title %}Projects{% endblock %}

{% block body %}
    <h1>Projects</h1>

    <table class="table">
        <tbody>
            
            <tr>
                <th>Title</th>
                <td>{{ project.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ project.content }}</td>
            </tr>
            <tr>
                <th>Objectif</th>
                <td>{{ project.objectif }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_projects_index') }}">back to list</a>

    <a href="{{ path('app_admin_projects_edit', {'id': project.id}) }}">edit</a>

    {{ include('admin_projects/_delete_form.html.twig') }}
{% endblock %}
